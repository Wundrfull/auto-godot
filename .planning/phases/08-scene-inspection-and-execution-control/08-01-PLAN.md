---
phase: 08-scene-inspection-and-execution-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gdauto/debugger/models.py
  - src/gdauto/debugger/session.py
  - src/gdauto/debugger/session_file.py
  - src/gdauto/debugger/__init__.py
  - tests/unit/test_models_phase8.py
  - tests/unit/test_session_phase8.py
  - tests/unit/test_session_file.py
autonomous: true
requirements: [SCENE-01, SCENE-02, SCENE-03, EXEC-01, EXEC-02, EXEC-03]

must_haves:
  truths:
    - "SceneNode, NodeProperty, GameState, SessionInfo dataclasses exist with to_dict() methods"
    - "DebugSession dispatches debug_enter/debug_exit messages to update game_paused state"
    - "DebugSession.send_command supports response_key parameter for request/response name mismatches"
    - "Session file can be written, read, and cleaned up from .gdauto/session.json"
    - ".gdauto/ is auto-added to .gitignore when session file is created"
    - "drain_output() and drain_errors() return and clear buffers"
  artifacts:
    - path: "src/gdauto/debugger/models.py"
      provides: "SceneNode, NodeProperty, GameState, SessionInfo dataclasses"
      contains: "class SceneNode"
    - path: "src/gdauto/debugger/session_file.py"
      provides: "Session file CRUD and .gitignore management"
      exports: ["write_session_file", "read_session_file", "cleanup_session"]
    - path: "src/gdauto/debugger/session.py"
      provides: "Enhanced DebugSession with debug_enter/exit handlers, response_key, drain methods"
      contains: "debug_enter"
  key_links:
    - from: "src/gdauto/debugger/session.py"
      to: "src/gdauto/debugger/models.py"
      via: "GameState import for pause state tracking"
      pattern: "from gdauto.debugger.models import GameState"
    - from: "src/gdauto/debugger/session_file.py"
      to: "src/gdauto/debugger/models.py"
      via: "SessionInfo import for serialization"
      pattern: "from gdauto.debugger.models import SessionInfo"
---

<objective>
Create the data models, session enhancements, and session file persistence that all Phase 8 commands depend on.

Purpose: Downstream plans (inspector commands and execution control commands) need SceneNode/NodeProperty/GameState/SessionInfo dataclasses, a DebugSession that tracks pause state via debug_enter/debug_exit messages, support for response_key mismatches in send_command, buffer drain methods, and session file persistence for auto-connect (per D-01 through D-04).

Output: Enhanced models.py, session.py, new session_file.py, all with tests.
</objective>

<execution_context>
@C:/Users/dared/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dared/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-scene-inspection-and-execution-control/08-CONTEXT.md
@.planning/phases/08-scene-inspection-and-execution-control/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/gdauto/debugger/models.py (current):
```python
@dataclass(frozen=True, slots=True)
class GodotStringName:
    value: str

@dataclass(frozen=True, slots=True)
class GodotNodePath:
    value: str
```

From src/gdauto/debugger/session.py (current dispatch):
```python
def _dispatch(self, command: str, data: list[Any]) -> None:
    if command in self._pending:
        self._pending.pop(command).set_result(data)
    elif command == "output":
        self._append_buffer(self._output_buffer, data)
    elif command == "error":
        self._append_buffer(self._error_buffer, data)
    elif command.startswith("performance:"):
        pass
    else:
        logger.debug("Discarding unknown unsolicited message: %s", command)
```

From src/gdauto/debugger/session.py (send_command):
```python
async def send_command(
    self, command: str, data: list[Any] | None = None, timeout: float = 10.0,
) -> list[Any]:
    ...
    self._pending[command] = future
    ...
```

From src/gdauto/errors.py:
```python
@dataclass
class GdautoError(Exception):
    message: str
    code: str
    fix: str | None = field(default=None)
    def to_dict(self) -> dict[str, str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data models and session file persistence</name>
  <files>
    src/gdauto/debugger/models.py,
    src/gdauto/debugger/session_file.py,
    tests/unit/test_models_phase8.py,
    tests/unit/test_session_file.py
  </files>
  <read_first>
    src/gdauto/debugger/models.py,
    src/gdauto/errors.py,
    src/gdauto/debugger/errors.py,
    .planning/phases/08-scene-inspection-and-execution-control/08-RESEARCH.md
  </read_first>
  <behavior>
    SceneNode:
    - SceneNode(name="Main", type_name="Node2D", instance_id=1, scene_file_path="", view_flags=0, path="/root/Main", children=[]).to_dict() returns {"name": "Main", "type": "Node2D", "path": "/root/Main", "instance_id": 1, "scene_file_path": "", "children": []}
    - SceneNode with nested children serializes children recursively in to_dict()
    - SceneNode with depth limit: prune_depth(node, 0) returns node with empty children; prune_depth(node, 1) keeps direct children but removes grandchildren

    NodeProperty:
    - NodeProperty(name="text", type=4, hint=0, hint_string="", usage=2, value="Hello").to_dict() returns {"name": "text", "value": "Hello", "type": 4}

    GameState:
    - GameState().to_dict() returns {"paused": False, "speed": 1.0, "frame": 0}
    - GameState(paused=True, speed=10.0, frame=42).to_dict() returns {"paused": True, "speed": 10.0, "frame": 42}

    SessionInfo:
    - SessionInfo(host="127.0.0.1", port=6007, game_pid=1234, project_path="/tmp/proj", created_at="2026-04-06T12:00:00Z").to_dict() returns expected dict
    - SessionInfo.from_dict(d) round-trips through to_dict()

    Session file:
    - write_session_file(tmp_path, info) creates .gdauto/session.json with valid JSON
    - read_session_file(tmp_path) returns SessionInfo when file exists
    - read_session_file(tmp_path) returns None when file missing
    - read_session_file(tmp_path) returns None when file contains invalid JSON
    - cleanup_session(tmp_path) removes session.json and .gdauto/ dir if empty
    - cleanup_session(tmp_path) keeps .gdauto/ dir if other files exist

    Gitignore:
    - write_session_file on project with no .gitignore creates .gitignore containing ".gdauto/"
    - write_session_file on project with .gitignore not containing .gdauto/ appends ".gdauto/" on new line
    - write_session_file on project with .gitignore already containing .gdauto/ does not duplicate
    - write_session_file on .gitignore without trailing newline adds newline before ".gdauto/"
  </behavior>
  <action>
    **models.py** -- Extend with 4 new dataclasses (keep existing GodotStringName/GodotNodePath unchanged):

    1. `SceneNode` dataclass (not frozen, mutable children):
       - Fields: name (str), type_name (str), instance_id (int), scene_file_path (str), view_flags (int), path (str), children (list[SceneNode])
       - `to_dict()` method: returns {"name", "type" (mapped from type_name), "path", "instance_id", "scene_file_path", "children" (recursive to_dict)}
       - Per D-05: JSON key is "type" not "type_name" in output

    2. `NodeProperty` dataclass:
       - Fields: name (str), type (int), hint (int), hint_string (str), usage (int), value (object)
       - `to_dict()` method: returns {"name", "value", "type"}

    3. `GameState` dataclass:
       - Fields: paused (bool, default=False), speed (float, default=1.0), frame (int, default=0)
       - `to_dict()` method: returns {"paused", "speed", "frame"}
       - Per D-12: all execution control commands return this

    4. `SessionInfo` dataclass:
       - Fields: host (str), port (int), game_pid (int), project_path (str), created_at (str)
       - `to_dict()` method and `from_dict(d)` classmethod for JSON round-trip
       - Per D-03: used by session_file.py

    **session_file.py** -- New module for .gdauto/session.json management:

    1. `write_session_file(project_path: Path, info: SessionInfo) -> None`:
       - Creates `.gdauto/` directory with `mkdir(exist_ok=True)`
       - Writes `session.json` with `json.dumps(info.to_dict(), indent=2)`
       - Calls `_ensure_gitignore(project_path)` per D-04

    2. `read_session_file(project_path: Path) -> SessionInfo | None`:
       - Returns None if `.gdauto/session.json` does not exist
       - Returns None if file contains invalid JSON (catch json.JSONDecodeError, TypeError, KeyError)
       - Returns `SessionInfo.from_dict(data)` on success

    3. `cleanup_session(project_path: Path) -> None`:
       - Removes `session.json` if it exists
       - Removes `.gdauto/` directory only if it is now empty

    4. `_ensure_gitignore(project_path: Path) -> None`:
       - If no `.gitignore` exists: create with `.gdauto/\n`
       - If `.gitignore` exists and already contains `.gdauto/`: do nothing
       - If `.gitignore` exists without `.gdauto/`: append (add \n first if file does not end with newline), then `.gdauto/\n`
  </action>
  <verify>
    <automated>cd C:/Users/dared/Documents/Ai/godot/godauto && uv run pytest tests/unit/test_models_phase8.py tests/unit/test_session_file.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - models.py contains `class SceneNode`, `class NodeProperty`, `class GameState`, `class SessionInfo`
    - SceneNode.to_dict() output uses key "type" (not "type_name")
    - GameState defaults: paused=False, speed=1.0, frame=0
    - session_file.py contains `def write_session_file`, `def read_session_file`, `def cleanup_session`, `def _ensure_gitignore`
    - write_session_file creates .gdauto/session.json with valid JSON
    - _ensure_gitignore creates/appends ".gdauto/" to .gitignore
    - All tests in test_models_phase8.py and test_session_file.py pass
  </acceptance_criteria>
  <done>All 4 dataclasses exist with to_dict(), session file CRUD works, .gitignore management works, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DebugSession enhancements (debug_enter/exit, response_key, drain methods)</name>
  <files>
    src/gdauto/debugger/session.py,
    src/gdauto/debugger/__init__.py,
    tests/unit/test_session_phase8.py
  </files>
  <read_first>
    src/gdauto/debugger/session.py,
    src/gdauto/debugger/__init__.py,
    src/gdauto/debugger/models.py,
    tests/unit/test_session.py,
    .planning/phases/08-scene-inspection-and-execution-control/08-RESEARCH.md
  </read_first>
  <behavior>
    debug_enter/debug_exit dispatch:
    - When session receives "debug_enter" message, session.game_paused becomes True
    - When session receives "debug_exit" message, session.game_paused becomes False
    - session.game_paused defaults to False on fresh session

    response_key parameter:
    - send_command("scene:request_scene_tree", [], response_key="scene:scene_tree") registers the future under key "scene:scene_tree" (not "scene:request_scene_tree")
    - When response arrives as "scene:scene_tree", the future resolves correctly
    - send_command without response_key continues to use command name as key (backward compatible)

    drain methods:
    - drain_output() returns list of buffered output messages and clears the buffer
    - drain_errors() returns list of buffered error messages and clears the buffer
    - After drain_output(), output_buffer property returns empty list
    - After drain_errors(), error_buffer property returns empty list

    current_speed tracking:
    - session.current_speed defaults to 1.0
    - current_speed is a public float attribute (set externally by execution control module)

    game_state property:
    - session.game_state returns GameState(paused=session.game_paused, speed=session.current_speed, frame=0)
  </behavior>
  <action>
    **session.py** -- Three targeted enhancements to DebugSession:

    1. **debug_enter/debug_exit handlers in _dispatch():**
       - Add `game_paused: bool` field (default False, init=False)
       - Add `current_speed: float` field (default 1.0, init=False)
       - In _dispatch(): add elif branches for "debug_enter" (set `self.game_paused = True`) and "debug_exit" (set `self.game_paused = False`) BEFORE the performance check
       - Add `game_state` property returning `GameState(paused=self.game_paused, speed=self.current_speed, frame=0)` (frame tracking deferred; always 0 for now)
       - Import GameState from models: `from gdauto.debugger.models import GameState`
       - Per D-12: this GameState is used by all execution control commands

    2. **response_key parameter on send_command():**
       - Add `response_key: str | None = None` parameter to `send_command()`
       - Change the pending key from `self._pending[command] = future` to `key = response_key if response_key is not None else command; self._pending[key] = future`
       - The pop in timeout cleanup must also use `key`: `self._pending.pop(key, None)`
       - This fixes the "scene:request_scene_tree" -> "scene:scene_tree" mismatch (Research Open Question 3)

    3. **drain_output() and drain_errors() methods:**
       - `def drain_output(self) -> list[list[Any]]`: copy `self._output_buffer`, call `self._output_buffer.clear()`, return the copy
       - `def drain_errors(self) -> list[list[Any]]`: copy `self._error_buffer`, call `self._error_buffer.clear()`, return the copy
       - These are needed by `debug output` command to consume buffered messages

    4. **Reset new fields in close():**
       - Add `self.game_paused = False` and `self.current_speed = 1.0` to the close() method

    **__init__.py** -- Add new exports:
       - Add `GameState`, `SceneNode`, `NodeProperty`, `SessionInfo` to imports and `__all__`
       - Add `write_session_file`, `read_session_file`, `cleanup_session` from session_file module
  </action>
  <verify>
    <automated>cd C:/Users/dared/Documents/Ai/godot/godauto && uv run pytest tests/unit/test_session_phase8.py tests/unit/test_session.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - session.py _dispatch() contains `elif command == "debug_enter"` and `elif command == "debug_exit"`
    - session.py send_command signature includes `response_key: str | None = None`
    - session.py contains `def drain_output` and `def drain_errors`
    - session.py contains `game_paused: bool` and `current_speed: float` fields
    - session.py contains `def game_state` property returning GameState
    - __init__.py __all__ includes "GameState", "SceneNode", "NodeProperty", "SessionInfo"
    - All existing tests in test_session.py still pass (backward compatibility)
    - All new tests in test_session_phase8.py pass
  </acceptance_criteria>
  <done>DebugSession handles debug_enter/exit, send_command supports response_key, drain methods work, all tests pass including existing ones</done>
</task>

</tasks>

<verification>
- All existing Phase 7 tests pass: `uv run pytest tests/unit/test_session.py tests/unit/test_debug_connect.py tests/unit/test_debug_cli.py -x`
- All new Phase 8 Plan 01 tests pass: `uv run pytest tests/unit/test_models_phase8.py tests/unit/test_session_file.py tests/unit/test_session_phase8.py -x`
- No import errors in debugger package: `uv run python -c "from gdauto.debugger import SceneNode, NodeProperty, GameState, SessionInfo, write_session_file, read_session_file, cleanup_session"`
</verification>

<success_criteria>
- 4 new dataclasses (SceneNode, NodeProperty, GameState, SessionInfo) with to_dict() methods
- Session file CRUD with .gitignore management
- DebugSession tracks game pause state via debug_enter/debug_exit
- send_command supports response_key for request/response name mismatches
- drain_output/drain_errors methods consume and clear buffers
- All existing Phase 7 tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/08-scene-inspection-and-execution-control/08-01-SUMMARY.md`
</output>
